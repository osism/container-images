---
name: Build nova-libvirt-cloud-hypervisor container image

"on":
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"
  push:
    paths:
      - .github/workflows/build-nova-libvirt-cloud-hypervisor-container-image.yml
      - nova-libvirt-cloud-hypervisor/**
    branches:
      - main
  pull_request:
    paths:
      - .github/workflows/build-nova-libvirt-cloud-hypervisor-container-image.yml
      - nova-libvirt-cloud-hypervisor/**

jobs:

  build-nova-libvirt-cloud-hypervisor-container-image:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version:
          - wallaby
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup docker
        uses: docker/setup-buildx-action@v1

      - name: Login to container registry
        uses: docker/login-action@v1
        with:
          registry: ${{ secrets.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
        if: github.ref == 'refs/heads/main'

      - name: Build container image
        run: scripts/build.sh
        env:
          DOCKER_REGISTRY: ${{ secrets.DOCKER_REGISTRY }}
          IMAGE: nova-libvirt-cloud-hypervisor
          REPOSITORY: osism/nova-libvirt-cloud-hypervisor
          VERSION: ${{ matrix.version }}

      - name: Push container image
        run: |
          scripts/push.sh
        env:
          DOCKER_REGISTRY: ${{ secrets.DOCKER_REGISTRY }}
          IMAGE: nova-libvirt-cloud-hypervisor
          REPOSITORY: osism/nova-libvirt-cloud-hypervisor
          VERSION: ${{ matrix.version }}
        if: |
          github.repository == 'osism/container-images' &&
          github.ref == 'refs/heads/main'
